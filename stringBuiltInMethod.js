const myString = "Zach";
// the replace method is a string method that takes in two arguement the string you want to replace and the string you want to replace with and returns a new string
let newMyString = myString.replace("h", "k");
console.log(newMyString);

let stringToUpper = "boneshaker";

var newUpper = stringToUpper.toUpperCase();
